◎概要

FPUの各モジュールを、bitwiseに答えが一致するようにC言語に翻訳したものです。

others.cにあるものは関数名はそのまま、それ以外は基本的にf〇〇→(先頭大文字)〇〇Floatのようになっています。

◎修正点

・itofが0または桁上がりするときに正しくない値を吐いていた。これを修正。

(後は軽微な修正です)

・名前の不一致を修正。(fsub→SubFloatなど)

・const_table, grad_tableの名前が衝突していたのでfinv_, sqrt_の形に変更。
